//characters in faction with notARealFaction = true (drifters, vagrant etc.) can't use public beds (in bars and such)
//solved by hooking into useablestuffs _NV_getCostToUse and set price to 0 or more if not a real faction

//USE_BED seems to make npc use whatever bed is available without needing to specify the subject
//ex: character->getOrdersReciever()->addOrder(USE_BED, nullptr, character->getPosition(), true, true);

//Order a character to go to bed (specify which bed)
//selectedCharacter->getOrdersReciever()->addOrder(USE_BED_ORDER, bed, bed->getPosition(), true, true);

//forcefully teleport a character into a bed
//character->setBedMode(true, bed);

//barmen constantly wake up from sleep if they have "get out of bed when in trouble" goal in their ai package

//can add hook to both TaskData::runTargetFind and AI::AIResultsCacher::runTargetFinder to mess with ai goal targeting